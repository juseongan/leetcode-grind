git process

      <-$stash clear      (hide $stash)               <-$reset
1. local    ->           2. staging               ->         3. remote
        $add        tracked  named($commit)     $push        accomplished
        $delete
